/**
 * Width of the content column a section lays its content out in, as the design
 * sets it. Anything measuring or clipping against that column reads it here, so
 * the column and the layout can't drift apart.
 */
export const SECTION_MAX_WIDTH = 1136;
