services:
  frontend:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  backend:
    build:
      context: ./api
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-0.19.0}
    env_file:
      - path: ./api/.env.example
      - path: ./api/.env
        required: false
    environment:
      CATALOG_PATH: /catalog/output
      # Container path, so it is pinned here rather than left to api/.env. The
      # export is only written when the SRA mirror is also configured, so this
      # being set on its own costs nothing.
      KMINDEX_EXPORT_DIR: /data/exports
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    volumes:
      - ../catalog:/catalog:ro
      # The SRA metadata mirror is a DuckDB file on the host, so it has to be
      # mounted in as well as configured. Set SRA_MIRROR_HOST_PATH to the
      # .duckdb file and SRA_MIRROR_PATH=/data/sra-mirror.duckdb in api/.env.
      # Both are needed: with only the variable set the path does not exist in
      # the container, and with only the mount the service stays disabled.
      - ${SRA_MIRROR_HOST_PATH:-/dev/null}:/data/sra-mirror.duckdb:ro
      # Writable, unlike everything else this service mounts: a kmindex search
      # materializes its full match set here so it can be downloaded
      # afterwards. Named rather than a bind mount so it survives a rebuild
      # without needing a host directory with the right ownership; the image
      # creates /data/exports so the volume inherits the runtime user's.
      # Retention is enforced in-process, not by the volume.
      - kmindex_exports:/data/exports
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  kmindex_exports:
  redis_data:
