# Redis Configuration
REDIS_URL=redis://redis:6379/0

# AI/LLM Configuration
#
# Two configuration approaches:
#
# 1. SINGLE MODEL (simple): Just set AI_MODEL
#    - Uses same model for both reasoning and formatting steps
#    - Good for: simple setups, powerful single models, development
#
# 2. HYBRID (cost-optimized): Set AI_REASONING_MODEL + AI_FORMATTING_MODEL
#    - Use expensive/smart model for reasoning, cheap/fast model for formatting
#    - Good for: production with API costs, optimal performance/cost balance
#
# If AI_REASONING_MODEL/AI_FORMATTING_MODEL are not set, falls back to AI_MODEL

# Example: Single model (Anthropic Claude)
AI_API_KEY=your-api-key-here
AI_API_BASE_URL=https://api.anthropic.com
AI_MODEL=claude-3-5-sonnet-20241022

# Example: Hybrid approach (uncomment to use)
# AI_API_KEY=your-api-key-here
# AI_API_BASE_URL=https://api.anthropic.com
# AI_MODEL=claude-haiku-4-5-20251001
# AI_REASONING_MODEL=claude-3-5-sonnet-20241022  # Expensive, smart
# AI_FORMATTING_MODEL=claude-haiku-4-5-20251001   # Cheap, fast

# Alternative: Use OPENAI_API_KEY (will be used as fallback if AI_API_KEY not set)
# OPENAI_API_KEY=sk-your-openai-key-here

# Database Configuration (for future use)
DATABASE_URL=postgresql://user:pass@localhost/dbname

# External API Configuration
ENA_API_BASE=https://www.ebi.ac.uk/ena/portal/api

# Application Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost
LOG_LEVEL=INFO
ENVIRONMENT=development

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60