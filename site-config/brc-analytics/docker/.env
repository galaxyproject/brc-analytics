NEXT_PUBLIC_ENA_PROXY_DOMAIN=""
# Docker builds deliberately reuse the "local" runtime environment (and its
# site config); only the env values in this file differ (nginx-proxied paths).
NEXT_PUBLIC_ENVIRONMENT='local'
NEXT_PUBLIC_GALAXY_INSTANCE_URL="https://test.galaxyproject.org"
NEXT_PUBLIC_GALAXY_ENV="TEST"
NEXT_PUBLIC_BACKEND_URL=""
