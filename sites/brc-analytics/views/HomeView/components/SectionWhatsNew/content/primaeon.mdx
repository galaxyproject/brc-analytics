### PrimAeon: selection analysis in your browser

Paste an alignment and get a selection report in seconds — no upload, no
install. PrimAeon runs the HyphAeon deep-learning models entirely on your
device, so your sequences never leave the browser.
